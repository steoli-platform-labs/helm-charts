apiVersion: v1
kind: Service
metadata: {name: {{ include "sample-api.fullname" . }}}
spec:
  selector: {app.kubernetes.io/name: {{ include "sample-api.name" . }}}
  ports: [{port: {{ .Values.service.port }}, targetPort: {{ .Values.service.targetPort }}}]
